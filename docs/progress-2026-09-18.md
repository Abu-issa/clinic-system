# Progress — 2026-09-18

## Clinical tests — Phase 2

Implemented multiple result files through ClinicalTestResultAttachment and one semantic upload
command that reuses private attachment storage. First upload sets Uploaded/UploadedAtUtc;
additional uploads preserve first-upload time and current Uploaded/UnderReview state. Dedicated
Doctor-only start/complete commands implement Uploaded -> UnderReview -> Reviewed, with server
reviewer/time and final immutability. SQL RowVersion protects all three mutations, including
additional uploads that do not change lifecycle values.

Upload permits Doctor/DoctorAssistant with tests.write AND attachments.write, MFA and exact patient
scope. Doctor authority uses live persisted association and Visit.DoctorId; assistants cannot
review. Detail exposes result metadata only with attachments.read and retains fail-closed access
audit. Downloads reuse the existing patient-bound attachment endpoint. CSRF/authority checks
precede body reading; multipart total and streamed feature limits produce 413.

One shared SaveChanges transaction includes StoredFile, PatientAttachment, result link, request
update and both upload audits. Review mutations commit with their audit. SQL failure/cancellation
compensates promoted bytes. Failed compensation retains the original error and leaves only the
documented invisible orphan-byte risk. A coordinated SQL race caught and fixed dependent/principal
detach ordering in the shared AttachmentStore cleanup; the loser now returns 409 and leaves no
rows, successful events or object. No concurrency behavior was weakened or replaced with retries.

Exactly one generated migration: `20260918114928_AddClinicalTestResultsAndLifecycle`, adding the
result-link table/FKs/indexes and evolving `CK_ClinicalTestRequests_Lifecycle`. Historical Phase 1
migration unchanged. ClinicDb schema/data untouched; the migration list was inspected read-only
and shows eight pending migrations, including this one. No migration application outside disposable
ClinicTests_* fixtures, no commit, no push, no UI and no Phase 3 work.

Feature design, security/transaction details, limitations and full file inventory:
[clinical-tests-phase-2.md](clinical-tests-phase-2.md).

**Implementation baseline before the final gate: READY FOR CLINICAL TESTS PHASE 2 PRE-COMMIT GATE**

Final verification:

- `git diff --check`: clean.
- `dotnet build backend/Clinic/Clinic.slnx --nologo -m:1 -p:UseSharedCompilation=false`:
  succeeded, zero warnings and zero errors.
- `dotnet test backend/Clinic/Clinic.slnx --no-build --no-restore --nologo -m:1 --verbosity minimal`:
  **805 passed: 269 unit + 536 integration; zero failed, zero skipped**. This is 71 more
  cases than the verified Phase 1 baseline of 734.
- `dotnet ef migrations has-pending-model-changes --no-build --project backend/Clinic/src/Clinic.Infrastructure --startup-project backend/Clinic/src/Clinic.Api`:
  no model changes since the migration.
- `dotnet ef migrations list --project backend/Clinic/src/Clinic.Infrastructure --startup-project backend/Clinic/src/Clinic.Api`:
  succeeded; the new migration remains pending alongside the prior seven pending migrations.

The full suite includes regression coverage for generic file storage/attachments after the shared
upload orchestration and cleanup changes. The new coordinated SQL race proves one successful
upload, one 409, loser-row/event rollback, promoted-object deletion and a successful refreshed-token
retry. Independent failure injection proves both upload audit insert rollbacks, review rollback,
metadata/link failures and compensation behavior.

## Final pre-commit hardening gate

**READY TO COMMIT CLINICAL TESTS PHASE 2**

Fixed one low-severity ownership issue: the lifecycle service and shared attachment upload core
both disposed the opened stream. Each public workflow now owns its input exactly once and the
shared core borrows it. Generic attachment behavior remains covered by the full regression suite.

Added 42 integration cases covering first/additional upload races, competing review commands,
completion winning over a promoted upload, immutable times/reviewer/order fields, base64 token
round-trips, read/count/download permissions, standalone live authority, assistant boundaries,
cross-patient lower-level rejection, stream disposal, duplicate links and SQL lifecycle INSERTs.

Final verification: **847 passed — 269 unit + 578 integration; zero failed, zero skipped**.
Build: zero warnings/errors. EF drift: clean. git diff --check: clean. The eight pending migrations
remain unapplied; ClinicDb schema/data untouched. Historical migrations unchanged. No commit,
push, UI or Phase 3 implementation.

Detailed findings, evidence, exact changed files, commands and pending migrations:
[clinical-tests-phase-2-final-gate.md](clinical-tests-phase-2-final-gate.md).

The subsequent Phase 3 planning decision retained file-first results. Structured result entry
remains deferred; the next authorized implementation slice is the patient-scoped Staff MVC workflow.

## Staff MVC — Clinical tests Phase 1

**READY FOR STAFF MVC CLINICAL TESTS PHASE 1 REVIEW**

Added list, create and detail Razor pages to the existing Clinic.Api web host (this checkout has
no separate staff MVC project). Pages reuse staff cookie/MFA/persisted grants, exact patient scope,
application services, protected downloads and existing audit events. No second login system,
HTTP loopback, DbContext in MVC, structured results or new lifecycle rules were introduced.

Arabic defaults to RTL with English support and locally vendored Bootstrap 5.3.8. Patient context
shows only name/MRN (opaque reference for legacy records). Safe attachment metadata requires
attachments.read; test-only readers retain counts. Doctor/assistant upload and Doctor review use
fresh RowVersion tokens and localized POST/redirect/GET feedback. The shared upload gate verifies
header CSRF and authority before consuming multipart bytes. Staff pages have no-store/security
headers and fail-closed read auditing.

42 new MVC integration cases bring the verified suite to **889 passed: 269 unit + 620 integration;
zero failed/skipped**. Build has zero warnings/errors; git diff --check is clean. Synthetic HTML
visual inspection covered Arabic list/detail and English create pages; live browser login/upload
automation remains deferred. Tests use disposable ClinicTests_* databases; ClinicDb untouched.
No MVC schema changes, commit or push. Prior Phase 2 working-tree changes are preserved.

Routes, permission matrix, workflow/CSRF/audit design, exact file inventory and validation:
[staff-mvc-clinical-tests-phase-1.md](staff-mvc-clinical-tests-phase-1.md).

Next: **STAFF MVC CLINICAL TESTS PHASE 1 REVIEW / HARDENING**.
