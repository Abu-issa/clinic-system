\# Development checkpoint — 2026-09-08



\## Implemented

\- Aligned appointment states with Pending, Confirmed, Arrived,

&#x20; InProgress, Completed, Cancelled, and NoShow.

\- Added arrival and visit-start transitions and revised completion rules.

\- Added Doctor and linked appointments to doctors.

\- Added missing/inactive doctor validation to booking.

\- Implemented EF Core patient, doctor, and appointment repositories.

\- Registered repositories, booking service, shared DbContext/unit of work,

&#x20; and system time provider.

\- Created ClinicDb on local SQL Server using Windows Authentication.

\- Generated and applied InitialCreate.

\- Added a per-doctor SQL application lock within a booking transaction.

\- Moved booking reads, overlap checks, and persistence inside the transaction.



\## Verification

\- 50 tests passed: 43 unit tests and 7 integration tests.

\- Integration tests use temporary databases on local SQL Server.

\- Verified migrations and table queries.

\- Verified one successful booking for concurrent overlapping requests.

\- Verified adjacent appointments and bookings for different doctors.

\- Verified rollback after saving but before commit, and successful rebooking.



\## Limitations

\- No booking HTTP endpoint yet.

\- Authentication and resource-level authorization are not implemented.

\- Working schedules, rescheduling, cancellation reasons, audit events,

&#x20; and idempotency remain pending.

\- All future availability-changing operations must follow the locking protocol.

\- Direct SQL writes do not automatically honor the application lock.

\- Testcontainers setup remains pending.

\- Patient medical-record numbering and the full clinical record remain pending.

\- Flutter, MVC, notebook, archive, and notifications have not started.



\## Next

\- Add API result/error handling and booking endpoints with an explicit

&#x20; authorization/development-access plan.

\- Verify HTTP behavior with integration tests.

\- Continue working schedules and booking policy implementation.



\## Local tooling

\- dotnet-ef 10.0.11 installed.

\- Visual Studio External Tools entries are local IDE settings;

&#x20; they are not automatically included in the Git repository.

