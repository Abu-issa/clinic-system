\# Development checkpoint — 2026-09-12



\## Implemented

\- Protected staff cancellation HTTP endpoint.

\- Doctor-scoped cancellation permission and CSRF validation.

\- Cancellation actor obtained from the authenticated identity.

\- Same-doctor rescheduling service for Pending/Confirmed appointments.

\- Rescheduling preserves the appointment's current status.

\- Working-hours, closures, and overlap checks excluding the appointment itself.

\- Rescheduling history saved with the appointment in one transaction.

\- Applied AddAppointmentReschedules and AddAppointmentRowVersion.

\- Expected RowVersion validation rejects stale requests.

\- PersistenceConcurrencyException translation and rescheduling handling.



\## Verification

\- 95 tests passed: 44 unit and 51 integration.

\- Verified cancellation HTTP persistence and authenticated actor identity.

\- Verified rejected cancellation requests do not modify appointments.

\- Verified cancellation removes appointments from current closure affected lists.

\- Verified rescheduling history, self-overlap exclusion, and conflict rejection.

\- Verified stale-version rejection preserves the first change.



\## Next

\- Test an actual save-time concurrency conflict and transaction rollback.

\- Verify no rescheduling history is persisted after the failed operation.

\- Add protected rescheduling HTTP endpoint and version-aware request/response.

\- Extend concurrency error handling to other mutation services.



\## Limitations

\- Save-time conflict handling is implemented but not yet directly tested.

\- Rescheduling HTTP endpoint is not implemented.

\- Real staff login, session revocation, and audit trail remain pending.

